# Worklog - 2026-09-03

## Branch

`feat/m1a-core-hardening`

## Summary

This branch continues after the first build-fix merge and keeps the work inside the M1a/M1b path.

Implemented:

- reduced false XML documentation noise for test and sample assemblies;
- improved storage preamble/BOM handling;
- added storage tests;
- added `Edit.DeleteRight` as a Core command;
- added dispatcher tests for the M1 command set;
- added mixed-line-ending and normalized-range buffer tests;
- improved the Modern FIRST-ED sample dirty guard and error display;
- exposed WinForms view-state notifications so the sample status bar can follow caret movement;
- added practical validation and next-step notes.

## Validation status

Not locally executed by the agent environment because no .NET SDK is available there. Human/local validation is required before merge.
