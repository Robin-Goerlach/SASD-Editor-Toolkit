# PR 2 Agent Limitations

The branch was edited through the GitHub connector. The connector can change repository files, but this environment cannot run the .NET SDK. Local build and test output from Windows remains the source of truth before merge.

No changes outside `Robin-Goerlach/SASD-Editor-Toolkit` were made.
